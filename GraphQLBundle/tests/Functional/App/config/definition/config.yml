imports:
    - { resource: ../config.yml }

overblog_graphql:
    definitions:
        schema:
            query: RootQuery
        mappings:
            types:
                - type: yaml
                  dir: "%kernel.project_dir%/config/definition/mapping"

overblog_graphql_configuration_yaml_xml:
    yaml:
        mapping:
            directories:
                - "%kernel.project_dir%/config/definition/mapping"
