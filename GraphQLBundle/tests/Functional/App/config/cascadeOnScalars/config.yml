imports:
    - { resource: ../config.yml }

overblog_graphql:
    definitions:
        config_validation: false
        class_namespace: "Overblog\\GraphQLBundle\\Validator\\__DEFINITIONS__"
        schema:
            query: Mutation
            mutation: Mutation
        mappings:
            types:
                - type: yaml
                  dir: "%kernel.project_dir%/config/cascadeOnScalars/mapping"

overblog_graphql_configuration_yaml_xml:
    yaml:
        mapping:
            directories:
                - "%kernel.project_dir%/config/cascadeOnScalars/mapping"
