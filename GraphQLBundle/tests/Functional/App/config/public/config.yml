imports:
    - { resource: ../config.yml }

overblog_graphql:
    definitions:
        class_namespace: "Overblog\\GraphQLBundle\\PublicField\\__DEFINITIONS__"
        schema:
            query: RootQuery
        mappings:
            types:
                - type: yaml
                  dir: "%kernel.project_dir%/config/public/mapping"

overblog_graphql_configuration_yaml_xml:
    yaml:
        mapping:
            directories:
                - "%kernel.project_dir%/config/public/mapping"
